apiVersion: v1
kind: Service
metadata:
  name: harness-course-app
  namespace: harness-course
  labels:
    app: harness-course-app
spec:
  type: LoadBalancer
  selector:
    app: harness-course-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
