# Development environment overrides

replicaCount: 1

image:
  tag: "latest"

service:
  type: ClusterIP

resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "256Mi"
    cpu: "250m"

autoscaling:
  enabled: false

env:
  APP_ENV: "development"
  LOG_LEVEL: "debug"
