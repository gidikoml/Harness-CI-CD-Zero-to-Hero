pipeline:
  identifier: Build_gidikoml_Harness_CI_CD_Zero_to_Hero_1783176241679
  name: Build Harness-CI-CD-Zero-to-Hero
  orgIdentifier: default
  projectIdentifier: HarnessCICDZerotoHero
  properties:
    ci:
      codebase:
        build: <+input>
        connectorRef: account.Github
        repoName: gidikoml/Harness-CI-CD-Zero-to-Hero
  stages:
    - stage:
        identifier: build
        name: build
        spec:
          caching:
            enabled: true
          cloneCodebase: true
          execution:
            steps:
              - step:
                  identifier: echo
                  name: echo
                  spec:
                    command: echo hello world
                  timeout: ""
                  type: Run
              - step:
                  type: Run
                  name: Run_2
                  identifier: Run_2
                  spec:
                    shell: Sh
                    command: |-
                      echo "=== Building Hello World ==="
                      cd Episode-01/hello-world-app
                      mvn clean package -q
                      echo ""
                      echo "=== Running the App ==="
                      java -jar target/hello-world-1.0.0.jar
                      echo ""
                      echo "=== Pipeline Success! ==="
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
        type: CI
