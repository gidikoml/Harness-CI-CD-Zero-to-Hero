apiVersion: v1
kind: Service
metadata:
  name: {{ include "harness-app.fullname" . }}
  labels:
    {{- include "harness-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "harness-app.selectorLabels" . | nindent 4 }}
