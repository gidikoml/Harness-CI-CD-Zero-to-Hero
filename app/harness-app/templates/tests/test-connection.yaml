apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "harness-app.fullname" . }}-test-connection"
  labels:
    {{- include "harness-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "harness-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
